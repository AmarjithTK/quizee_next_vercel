{
  "name": "quizzee",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next build && next export && git add out -f && git commit -m 'Deploy Next.js static site' && git subtree push --prefix out origin gh-pages",
    "export": "next build && next export",
    "deploy": "rm -rf out && next build  && gh-pages -d out -b gh-pages"
  },
  "dependencies": {
    "next": "15.2.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4"
  }
}
